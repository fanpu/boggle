import { LabelScorePipe } from './label-score.pipe';

describe('LabelScorePipe', () => {
  it('create an instance', () => {
    const pipe = new LabelScorePipe();
    expect(pipe).toBeTruthy();
  });
});
